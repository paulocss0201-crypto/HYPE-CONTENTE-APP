// Single source of truth for layout spacing, so every generated composition
// shares the same margins instead of each layout function inventing its own.
export const MARGIN = 0.08;
export const GUTTER = 0.04;
